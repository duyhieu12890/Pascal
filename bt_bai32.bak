uses crt;
var a:array[1..100] of longint;
        i,n:integer;
begin
        write('co may so nguyen ?, cau tra loi:');readln(n);

        for i:=1 to n do
        begin
                write('so thu ',i);
                readln(A[i]);
        end;
        for i:=1 to n do
        if A[i] < 0 then write(A[i],' ');

        writeln;
        for i:=1 to n do
        if A[i] > 0 then write(A[i],' ') else write('0');
        readln;
end.
