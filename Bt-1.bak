uses crt;
var a,b:array[1..100] of integer;
    i,j,n,max:integer;

begin
        write('co bao nhieu n so duong?');readln(n);
        for i:=1 to n do
         begin
                 write('so thu ',i,':');
                 readln(A[i]);
         end;
        max:= a[i];
        for i:=2 to n do
                if max < a[i] then max:= a[i];
        for i:= 1 to n do
        inc(b[a[i]]);
        for i:=1 to max do
        if b[i]> 1 then writeln(i,' ',b[i]);
        readln;
end.