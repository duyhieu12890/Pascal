uses crt;
var A:array[1..100] of longint;
        i,n:integer;
        x:longint;
begin
        write('co may so nguyen:');readln(n);
        for i:=1 to n do
                begin
                        write ('so thu ',i,' ');
                        readln(a[i]);
                end;
        write('so nguyen x la:');readln(x);
        writeln('so nho hon x la');
        for i:=1 to n do
        if A[i] < x then write(A[i]);
        readln;
end.
